# Preparation calibration verification

## Source and ownership

Initial source commit: `b0db886d6418f8a2d539f20575db6cea803a8a8f` on `codex/trusted-preparation-calibration`,
isolated in `/Users/masonwyatt/.codex/worktrees/ashlr-hub/firm-calibration`.
Integrated source: `67f662fc0ef8e1eee6ffce072b99a217120d7559` on `auto/p00`
in `/Users/masonwyatt/.codex/worktrees/ashlr-hub/firm-p00`.
The original Desktop checkout and previous workplans were preserved. Three agents
built/reviewed disjoint core modules and tests; the parent integrated CLI, SDK,
documentation and commits.

Final source hardening: `03a12d48a8b747b56a411164cb2ee9e5ba2de94e`, integrated
as `713bff6c2f787861975c4e1243beb0275bfa1e4c`. Recorded evaluator launch arguments
must now match the fixed installed tuple before normalization. Its six new pure
regressions forge the same invalid command across all baseline captures, avoiding
a false sense of safety from cross-capture equality alone.

## Verified behavior

- Read-only calibration from three distinct complete, settled captures with
  identical artifact, installed workload, manifest/comparator and region counts.
- Bounded deterministic descriptor with capture hashes and full artifact inventory.
- Captured candidate joins with original artifact identity, exact non-target file
  content and executable flags, and final journal/seed observations.
- Strictly lower aggregate broker processes and no process/blob region regression
  for an `improved` diagnostic. No score, acceptance or delivery authority.
- Public CLI and SDK paths; closed options, redacted errors, bounded descriptor
  reads, unknown-versus-zero arithmetic and no automatic execution/retry.

## Validation

At the initial integrated source, a combined serialized gate passed **465 tests
in 15 files, zero skipped, 16.33 seconds**. It included pure comparison/calibration,
CLI routing/inputs, report and capture regressions, real filesystem comparison,
installed registry, native trial custody, native diagnostic capture and real
descriptor reads. Builds, source/web typechecks, scoped lint, documentation links
and real-I/O lane checks passed. Compiled help for both new commands returned 0.
Full repository lint also returned 0 with 107 warnings in unrelated files;
the touched-file lint checks had no warnings.

The final rebuilt integrated source passed **474 tests in 15 files, zero skipped,
16.68 seconds**. The final filesystem suite contains 33 cases, including one
actual immutable-writer → calibration CLI → explicit descriptor file → comparison
CLI path. Compiled SDK imports verified all three public functions are available.
All test handles were terminal before this handoff.

The filesystem round trip used actual CLI functions, private journal files and
the real codec; measurement contents were synthetic, not native baseline proof.
The first fixture version was refused because it lacked required staging and
canonical record newlines. Correcting fixtures yielded 30/30 passing cases;
production reading rules were not relaxed.

## Limits and live state

The descriptor is caller-supplied diagnostic input. Its parser cannot authenticate
its author, and historical receipts do not establish current account health.
Candidate-linked during-call qualification, installed scoring authority, three
actual successful baseline captures and accepted optimization remain outstanding.
No full-success calibration or self-improving fleet operation is claimed.

Read-only global KILL observation on this turn: `state=active`,
`sourceState=healthy`. It was not cleared or redirected. No provider/account
changes, service activation, external publication or GitHub Actions occurred.
Entire reported enabled/manual-commit; branch resume had no prior checkpoint.
