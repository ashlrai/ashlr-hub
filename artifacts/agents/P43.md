# P43 — Runnable inert firm demo
Implements a fixed local graph: plan, implementation, cold verifier and signed traces.
The plan records two distinct alternatives for a pinned four-row boolean XOR spec.
The implementation writes declarative data; no candidate source is executed.
The independent checker ignores a forged passing log and rejects the planted liar.
The planted conflict references the positive verification trace and survives query.
Fixture acceptance is separate from graph completion: one node is intentionally rejected.
CLI demo/status/query require an explicit existing private root; help requires none.
Runtime reuses the existing provenance key and never creates a new trust root.
This is in-process fixture evidence, not model isolation, live autonomy or release proof.
