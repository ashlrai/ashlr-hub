# P37 — resource worker shared KILL enforcement
Scope: resource worker transport boundary only; existing ~/.ashlr/KILL authority is reused.
Entry: executeResourceWorker in src/core/resources/worker.ts.
Admission: active, unknown or unreadable KILL stops transport before start.
Active work: 50 ms polling forwards one AbortSignal into existing native cleanup or local fetch.
Receipts: original completion, usage, timeout and uncertain-cleanup evidence is preserved.
Cancellation waits for existing settlement; it does not prove no requests or spending occurred.
Limits: polling is not hard real-time; native Windows dispatch is explicitly refused pending Job Objects; local HTTP remains supported.
Verification: 141 targeted tests passed (12 new); scoped ESLint, full typecheck and diff check passed; parent owns real-I/O registration.
Trace: P37.trace.json is unsigned metadata; no services, providers, authority roots or credentials activated.
