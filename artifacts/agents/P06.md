# P06 — Evidence-pinned value allocation caller
Status: integrated on local auto/p00 with independent source review; no activation authority.
API: createValueAllocationReceipt(input, options) and verifyValueAllocationReceipt(receipt, trace, keyOptions).
Exact UTF-8 vision and mission pins, canonical hypothesis pin and existing resource-envelope digest are checked before scoring.
The existing pure portfolio retains its 12-candidate, 3-active-bet, reserve and concentration rules unchanged.
Receipts bind policy metadata and portfolio output; input bytes are caller-pinned, not externally observed or semantically verified.
DecisionTraceV1 uses only the existing provenance key by default; missing keys return unavailable without initialization.
Outcome evidence requires the existing caller-owned verifier interface; this caller never claims independent observation.
No dispatch, lease, budget renewal, persistence, model call, provider call, source mutation or promotion is implemented.
Verification: 22 focused tests and 80 combined regressions pass; full typecheck, scoped ESLint and diff checks pass.
