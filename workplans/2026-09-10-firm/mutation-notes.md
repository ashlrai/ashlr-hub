# Candidate-linked during-call drift

Baseline: df44cf730f4ca4155e8c56e0a8ce1783f630a0d0, clean auto/p00.
Entire resume found no checkpoint. Three Explore agents mapped exact boundaries.

Correction to the preceding proposed design: inspectPrivateDirectory itself does
not broker an ACL operation. The exact intent.json ACL read is an observable
boundary after initial capture, generated evidence and successful receipt equality.
Use it for runtime and delivered-branch mutations before returning the unchanged
settled tool result to the child. Match semantic request fields, not a global
operation count. Injection is trusted fixture work, never candidate measurement.

Runtime stale control removes the final capture/digest comparison only. Source
stale control retains initial delivered-origin verification but removes later
assertSource revalidation. Both must return full identical metadata while healthy;
the real baseline must then refuse during-call mutation while stale controls
demonstrate why that refusal matters. A final-return-only source guard control
requires another semantic boundary; an ordinal of five repeated branch reads is
not adopted as a candidate-independent requirement.

Ownership: graph agent owns test-only interceptor and deterministic unit controls;
execution agent owns runtime drift tests; resources agent owns successor drift
tests; parent owns private candidate harness, local gates, documentation and commit.
No native tests in parallel; no model/provider execution or real account changes.

Review found unconditional harness teardown could erase evidence after an
unconfirmed close. Session custody now starts before each attempted open and is
released only after successful close. Failed opens/closes retain the private root;
all concurrent close callers share the same promise. Fake-session controls cover
this separately from actual process tests. An early strict-test compile also
found a zero-argument mock inferred as an empty call tuple; explicit runner
argument types fixed that test-only issue without changing semantics.

Final fresh gate: 178/178 tests, eight suites, zero skips, 271.12s on macOS and
Node 24.18.0. It includes both real runtime cases, both real successor cases,
31 interceptor controls, 11 custody controls, existing deadline/readonly/protocol
checks and seven production metadata regressions. Healthy metadata is exact;
baseline refusal and stale-candidate success both pass after deliberate mutation.

Full build, source/web typechecks, strict new-test compilation, documentation
checks and lint pass (zero errors, 107 existing warnings). Lane guard reports
330 real-IO files and 681 unit files. Documentation check: 115 local links,
31 source links, 36 external links, zero network requests, no errors.
No runtime source/evaluator script changed; rebuilt manifest SHA-256 remains
c204cc7d483b504c035e4273b739d3c3950d89ed26d4ea5b0cb966946fe19ab3.

Next: finish remaining candidate-linked matrix and integrate the full workload
into the installed evaluator before freezing a reward. These results demonstrate
test sensitivity to two stale-evidence bugs, not accepted autonomous improvement.
