# Full-workflow measurement findings

Baseline: 2acf37714ee01135550de75d33de0a648ac4ce54, clean auto/p00.
Entire resume found no checkpoint. The original working checkout is preserved.

The prior increment is verified progress, not completed autonomy. Next work must
exercise candidate changes through real manager/restart/successor paths, rather
than measuring a frozen baseline accidentally imported by nested dependencies.
Three agents mapped workload fixtures, substitution and independent controls.

Implementation ownership: fixed workflow facade (resources agent), exact candidate
module substitution and child dispatch (execution agent), readonly command grammar
and cumulative launch ledger (graph agent), real-process fixture integration and
independent controls (parent). The fixed workflow source is embedded in the existing
bridge: no candidate-selected build, no extra runtime dependencies or baseline fallback.
Manager restoration runs inside an explicit measured request. The supervisor view
is deliberately nonexecuting; actual manager and enrollment validation are retained.
This is workflow measurement infrastructure, not a frozen optimization reward.

Initial runtime findings: test fixture work/candidate roots must be disjoint;
the production guard correctly refused the first overlapping setup. The real
manager profile uses a literal filtered for-each-ref read not covered by the old
leaf broker. The exact read was added, with expanded queries/writes still refused.
The first full manager session exhausted its 120s deadline after 1453 observed
native operations, with open/check replies but no replay completion. Explicit
workflow sessions initially received one 240s deadline; normal 60s defaults, installed
110s evaluator allowance and 30s tool ceiling are unchanged. No call renews time.

Complete successor protocol sequence then reached 2639 native operations and all
three read replies in 231.4s. The final explicit workflow allowance is 300s, with
headroom for real-IO variation rather than a change to acceptance or tool limits.
Full report comparison also exposed a trusted fixture mismatch: setup still held
a supervisor lock and had a signing key in its home. Expected reports are now read
after setup closure in an empty private home matching the candidate context;
commissioning fields remain compared, not normalized away.

Final independent review tightened dispatch and close against the same monotonic
deadline. Initial/tool confinement time is deducted before launch; cancellation
or expiry during confinement refuses launch, and a clean process settlement after
expiry cannot validate close. Six deterministic fake-bridge tests pass separately
from real native execution. Startup is now included in private fixture snapshots.

Fresh corrected manager baseline: two complete candidate processes pass exact
output/count equality and whole-invocation snapshots in 297.04s combined. Per
sequence: open 708 operations (6 blob); full bundle 237 (2 blob); check 264
(2 blob); replay 513 (2 blob). Total 1,722 validated broker operations, 12 blob
operations. These are fixed-fixture observations, not a frozen reward or speedup.

Fresh successor baseline: two complete candidate processes pass exact output/count
equality and whole-invocation snapshots in 474.26s combined. Per sequence: check
700 operations (7 blob); metadata 909 (8 blob); full bundle 1,030 (8 blob).
Total 2,639 validated broker operations, 23 blob operations. The delivered source
checkout and absence of a successor execution ledger are preserved.

Independent performance map: one profile, two restoration reads, check and replay
expand to ten target capture calls and four commissioning reports, plus owner
validation outside the mutable target. This is a source map, not an attribution
of the measured 1453 operations. Target-only batching of distinct evaluator blob
OIDs is the clearest future optimization; one-file fixtures may gain nothing.
Moving pure malformed-input validation earlier helps negative paths only. Larger
campaign/Universe projection reuse would require a separately authorized mutable
dependency scope and freshness/mutation controls; never cache authority across calls.

Independent next-gap audit: existing production suites cover several mutation and
publication boundaries, but do not substitute a selected candidate. The candidate
matrix still needs during-call runtime/pool/binding/project replacement; same-size
and restored-mtime executable/comparator/receipt/intent mutations; final successor
artifact/delivery-receipt drift; General/Spark/unknown-evidence preservation; benign
observation refresh; selected nondefault projects and unrelated history; and repeated
identical successes within one process. Two fresh workflow repetitions are baseline
evidence, not the three required runs for a frozen score. Expanded installed dispatch,
byte diagnostics and independent descendant-process observation remain unfinished.

Concrete next test seam: wrap the trusted test bridge's
`runVerifySubprocessAsync`, call through unchanged, and mutate only the private
fixture after a selected successful tool result settles but before the controller
publishes it. Match semantic command/path evidence, never a global operation
ordinal; assert exactly one injection and keep injection work outside candidate
measurement. No new candidate callback or production mutation hook is required.
First controls: mutate runtime after the bundle-directory ACL read that follows
initial capture; move the delivered source branch after its exact successful
`rev-parse --verify` read. Reuse the real metadata-suite mutation conventions,
compare preserved intentional mutation plus no additional effects, and require
normal child settlement. This is a reviewed next-step design, not implemented
coverage in the current gate.
