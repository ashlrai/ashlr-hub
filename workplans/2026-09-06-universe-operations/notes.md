# Operational discovery notes

The primary checkout remains at `a01fc086` with pre-existing untracked workplans.
The isolated Universe worktree is clean and starts from merged PR #355.

Last increment proved a three-generation local-model campaign: 9,580 reported
tokens, 5,236 checks passed each, one archive admission and two identical ties.
It did not prove new production product value or resident restart behavior.
# Repository delivery console

Extend the established Universe console, not a redesign. Reuse the system's
white canvas (#ffffff), slate text (#101828), indigo links (#5b6ff2), green
confirmed-state (#17794f), and amber incomplete-state (#a15200), including their
existing dark-mode aliases. Keep the existing sans UI typography and monospace
only for literal Git commands/identities. Left-align branch receipt rows under
the population archive; native disclosure reveals detailed provenance. Do not
turn branch delivery into a green production counter. No motion or decoration
is needed. Unlike a generic metrics dashboard, this section's job is to make an
evaluated trial traceable to its exact local commit and available next command.

## Publication state checked 2026-09-06

The local read-only `npm whoami --registry=https://registry.npmjs.org/ --json`
returned E401 Unauthorized. Both `latest` and `candidate` still resolve to
3.3.2. GitHub Actions repository permissions remain `enabled:false`. Do not
represent a local package or source merge as a new npm release. Publication
requires restored maintainer authentication and a compatible local publishing
path; the frozen 3.3.2 OIDC workflow is not such a path.

## Review fixes

- Delivery receipt inspection must bind back to historical selected trial and
  manifest/comparator evidence, not Git objects alone. Core/independent-review
  agents both identified this and are adding validation and regressions.
- Console now navigates to both run and trial IDs. Multi-niche generations no
  longer open the first selected trial when a different delivery was selected.
- Full web suite: 192 tests / 33 files passed before final core integration.
