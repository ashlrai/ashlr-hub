# Universe findings

The supplied vision describes five engines: population, organization, world model, science, and economy. The requested outcome is a functioning evolution loop rather than a larger approval inbox.

Current baseline: origin/master d1f007d0, package version 3.4.0. The primary checkout is older and contains unrelated untracked workplans. A new clean worktree owns this implementation.

Existing north-star grounding feeds strategist and invent via docs/NORTH-STAR.md. Existing source includes mission compilation, value portfolios, best-of-N, model routing, provenance, and agent-OS epoch records; discovery will determine reuse rather than replace them.

Entire resume on the primary branch found no checkpoint. No update to Entire or authentication is required for this build.

## Research and documentation

The full supplied attachment was read. Primary papers and original experiment artifacts support archive-based search, per-model harness selection, fixed held-out evaluation, and task-sensitive coordination. Results from company simulations, reasoning benchmarks, and Core War are scoped to those environments.

The most actionable measurement issue comes from Niklaus's harness study: changing cost weights while reusing a cached incumbent score caused an invalid promotion. Candidate and incumbent comparisons need matching objective/evaluator conditions and raw dimensions. DGM also documents objective hacking through changes to the logging behind a proxy metric.

`docs/ASHLR-UNIVERSE.md` defines the five-engine destination and stages from the local experiment kernel to later model, product, customer, and economy integrations. `docs/UNIVERSE-RESEARCH.md` records primary URLs and evidence limits. Current-behavior claims were checked against the runner, store, demo, CLI contract, README, and north star. Runtime execution is currently macOS-only, for operator-supplied commands with denied network access; timeout/cancellation owns a process group and does not establish termination of detached descendants. Wall time is measured; tokens and dollar cost remain null.

Independent real-I/O verification: `/opt/homebrew/opt/node@24/bin/node node_modules/vitest/vitest.mjs run test/universe-demo.test.ts --reporter=verbose` passed 1/1 on 2026-09-06 (4.34 seconds for the test, an observation rather than a performance promise). It verified two generations, two surviving niches, parent lineage, a rejected incorrect candidate, evaluator-derived scores, immutable artifact digests, and a healthy persisted projection. Focused ESLint, local documentation link/fence checks, and `git diff --check` passed. The integration owner is verifying packaged CLI and full-suite behavior separately.
