# Standing mission execution

## Goal

Move toward continuous useful engineering: an explicitly configured standing
mission runs fresh bounded campaigns through existing execution paths, preserves
rolling resource ceilings and prior settlement, and retains operator control.

## Phases

- [x] Inspect current clean primary and previous verification.
- [x] Map actual caller, accounting and recovery seams with three Explore agents.
- [x] Select the existing-runtime prerequisite; request campaign-sequencing guidance.
- [x] Implement scoped setup/console storage and integrate its actual caller tests.
- [x] Independently review and record tested behavior and remaining gates.

## Constraints

Primary auto/p00 at357f8c35; original dirty checkout untouched. Previous turn was
progress: sourcec3c2f1ab,278focusedtests and clean build; native76456terminal.
No live native test remains. Global stop and collector custody are not changed.
No providers, account policy mutations, resident service activation or public
publication are implied by building this capability. Never renew existing
deadlines, reset usage, or replay uncertain old work.

## Current work

Parent maps CLI and setup contracts. Runtime Explore maps actual executor reuse;
resource Explore maps cross-envelope accounting; independent reviewer maps legacy
mission components and adversarial restart conditions. Scope selected from source.

## Decision

The first prerequisite is explicit scoped preparation histories on one unchanged
account ledger. The optional host-pinned registrationScope travels existing
setup/profile/background paths; default legacy storage and digests are unchanged.
Each selected scope retains 32 records; recreating the owner loads only that scope.
The shared private store anchor, global ownership and accounting stay unchanged.
This is an actual setup/console capability, not yet automatic mission rollover.
The sequential-campaign architecture question is pending; no resident activation.

## Errors

Initial guessed CLI glob did not exist; switched to rg file discovery. One
guessed preparation-owner filename did not exist; actual owner is
console-engineering-preparation.ts. A plan patch anchor did not match; corrected
after reading this file. No source state changed on failed reads or patch.
A CLI-help patch had a case-sensitive anchor mismatch and was corrected before
applying. A web command used guessed nonexistent test paths and collected zero
tests; that run is not validation. Discovered actual test files with rg and ran
them without passWithNoTests.

## Completed increment

Source694b568e is committed and built cleanly (64056).186distincttests passed
across12files; all gates are terminal, including actualscopedCLI42535.
The full autonomous-fleet goal remains active. Next: predecessor-settlement
reader/mission caller and retained shared-ledger history, then commissioning.
