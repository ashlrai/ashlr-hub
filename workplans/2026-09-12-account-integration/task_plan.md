# Account integration

## Goal

Integrate the verified Spark proposal and passive collector inspection into the
primary runtime without changing the source underneath native acceptance, then
identify the shortest real path to account operation.

## Phases

- [x] Confirm primary and isolated branch identities and existing native handle.
- [x] Dispatch independent integration, account-operation and native-coverage reviews.
- [ ] Finish the existing native gate and record its actual outcome.
- [x] Fix isolated native test evidence retention and independently review it.
- [x] Run the four missing same-call native stale-result controls against the frozen bundle.
- [ ] Integrate the two ordered changes after the gate terminates.
- [ ] Run combined local acceptance and retain exact remaining activation blockers.

## Decisions

- Preserve native session 73003 and primary source/build while it runs.
- No provider requests, account mutation, stop bypass, quota refresh or publication.
- Use existing additive migration and quota policy, not a replacement scheduler.
- Keep original dirty checkout and all account history intact.

## Errors and session context

- Entire resume found no checkpoint on codex/spark-enrollment-preparation.
- A discovery command used a nonexistent shell glob; rerun with rg file discovery.
- A source inspection preceded new agent file publication; missing helper paths
  were informational, then reviewed after publication.
- Full isolated build42583 completed TypeScript, assets, builtin and257-module web
  build, but failed release dependency inventory with npm pack file report invalid.
  Investigating local dependency topology; do not call this a full build pass.

  Read-only trusted npm diagnostic64774 confirmed491escaping paths among3698rows,
  all from npm resolving the isolated dependency symlink into primary. After
  isolated native82818 settled, copied primary dependencies locally using APFS
  copy-on-write and preserved the old symlink in a private artifact. No install,
  registry request or validator relaxation.

## Status

Three read-only reviews are running. Native session 73003 remains live; its test
progress is not terminal acceptance. No integration has occurred yet.

Integration review found no conflicts. Source audit found an existing native-test
cleanup gap: early failure could delete unresolved evidence and later cancellation
could clear an earlier retention decision. A test-only isolated fix is underway.
Four same-call packaged qualification controls are running on primary handle3245;
these are separate from73003 and do not rerun the full frozen baseline. Both
handles keep primary source/dist frozen until terminal results are recorded.

Account regression90524 is terminal0:349tests/15files/zero skips54.34s.
Custody helper plus filtered real non-dispatch tests26155 is terminal0:
22passed/4explicitly filtered/2files2.32s. Independent review found no blocker.
The isolated regenerated evaluator has the exact same nine asset hashes and
25d57874 aggregate as primary. Lane check passes347real-IO/708unit files.

Changed native failure/cancellation paths passed in isolated gate82818:
2passed/13explicitly filtered/70.62s. The root is conservatively retained if any
invocation lacks complete proof; retained roots are reported for diagnosis.

Independent local dependency copy completed66991exit0. Full isolated build5553
now passes, including release dependency inventory, build identity,257-module web
build, scoped lint and docs. No source/package validation was weakened. This is
a local source build, not production deployment or native account commissioning.

Primary3245 is terminal0:4passed/1filtered/362.58s. All four current/stale runtime
and source qualification controls passed against unchanged25d57874; all nine
asset hashes independently rechecked afterward.73003remains the only live gate.
