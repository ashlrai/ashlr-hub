# Extraction and scoring constraints

- Current main owns module-global failure/checkpoint/Git/activity/abort state.
  Move operation state into one invocation. Import must not run the CLI.
- expectedInEmptyHome temporarily changes environment synchronously; no concurrent
  workload execution. Fixture runtime also allows only one active scope.
- Shared workload returns provisional diagnostic data; outer entry owns all final
  activity completion and stdout. Scoring cannot publish before custody settles.
- Preserve standalone leaf8 behavior, installed workflow19+qualification4 behavior,
  exact argument validation, fixed bridge/sidecar paths and original wall/mono time.
- Freeze all scoring-capable code before baseline capture: bridge includes
  reachable registry/store/runtime dependencies. Calibration pins measurement
  identity, while scoring identity separately pins its entry and calibration.
- Existing diagnostic envelope is refused by campaign scored-seed ingestion;
  do not claim seed measurement or autonomous scoring until that new route exists.

## Verified extraction checkpoint

Commit21343f3b contains the six-file extraction and tests. Independent normalized
loop review found only explicit Git/signal threading in the operations and catch
path; counters, qualifications and awaited closure are preserved. The69-test
focused gate and full build passed. Actual installed import exposed both functions
without starting execution. Native success of these new installed bytes remains
unverified; qualification gate41730 exercises its earlier parent bundle.

## Next bounded implementation packet

Use a fixed preparation-score entry and a closed registry ID. Package calibration
plus a byte-identical nested nine-file diagnostic bundle and its original manifest.
Import runPreparationWorkload, not the diagnostic adapter. Reuse the calibration
parser, scenario vector extraction and nonregression comparison; extract the pure
whole-inventory check from captured candidate comparison. Check source scope and
native pins before and after execution. The score entry owns one activity and
completion, and failures must never produce an improved score.

Budget support is a required dependency: the currently shared900-second worker
and evaluator clock cannot run the measured928-second workload. Preserve legacy
semantics when an explicit worker-phase field is omitted; introduce narrow closed
scoring support with whole-trial, phase and original campaign deadline checks.
Keep diagnostic-only registration and scored evaluation distinct.

## Scoring implementation verification in progress

The fixed score owner, pure decision/inventory module, explicit calibrated
packager and closed registry selection are implemented. Optional worker phase
budgets and deadline rechecks extend the existing runner, with a separate worker
ceiling in the workspace inspector. Scored manifests require minimizing the
integer preparation_processes metric. Real calibration is not yet installed.

Initial combined gate passed800 tests/24files/zero skips/47.52seconds. A separate
runner/model/campaign/integration gate passed129/6files/zero skips/45.99seconds;
browser projection tests passed54/2files/zero skips/721ms. Source/web types, docs,
lane checks and full build passed. These precede final review corrections and
must be rerun where affected; they do not establish native scoring acceptance.

Independent reviews found a registry file-read resource bound to tighten before
freezing bytes. Compiled-entry tests use actual package imports and native identity
reads but synthetic workload/calibration fixtures, never accepted baseline data.
The next full native gate must exercise the new shared diagnostic bytes.

## Errors and recovery

- A source-import lifecycle test could not resolve deliberately installed-only
  measurement sidecars. Removed that new test and moved the cases to the existing
  actual compiled-entry fixture instead; no production alias/workaround added.
- Entry lint required explicit globalThis for Node web globals; corrected and
  scoped lint passed.
- Initial archive.ts lookup was wrong; selection is in universe/store.ts. No
  archive or selection code was changed based on the guessed path.

## Final focused verification

Package owner tests now exercise settled success with synthetic measurements,
unsettled completion overriding provisional success, scope rejection before the
workload and directory replacement between publications. They do not prove a
native calibrated score. Parent reran package/scorer/budget/registry: 183 tests,
four files, zero skipped, 2.05 seconds. Native session 87267 remains the original
running diagnostic gate; installed bytes have not been rebuilt.

The pure scorer also refuses any vector drift for the unchanged baseline digest,
so lower or redistributed counts cannot masquerade as useful source changes.
The prepared three-capture driver has not run. A read-only current KILL check
found healthy/active/present; the prior retained attempt contains only a failed
preflight, not a capture, calibration or accepted result.

## Passing-seed delivery gap

Current selectWinners leaves a first selected trial parentTrialId/delta null.
verifiedInitialCampaignRepair only recognizes measured failed seeds; normal
delivery requires a positive archive-parent delta. Thus a passing seed150 and
first changed candidate149 need a distinct passed-seed proof for one-generation
delivery, not a fake failed seed, archive parent or preliminary no-op generation.
Implementation and actual private-record/Git acceptance are being coordinated.

The native gate remains on original session87267. Scoped campaign source edits
do not replace installed bytes; any later rebuilt bundle has its own identity.

Exploration errors: the installed directory is builtins/preparation, not
builtins/preparation-measurement. Searching generated bridge lines without a
column limit also returned embedded source; subsequent compiled searches must
use bounded matches. No code changes followed from the guessed path.

## Native full-workload result (control cases still live)

Session87267 returned a passing full workload at the existing frozen identity:
23 checks, both qualifications, 15 regions and4828 benchmark processes. Evaluator
elapsed933644ms; full test939752ms. Transport exit0, no signal/error/stderr,
timeout/cancellation/truncation false, confirmed group exit. Report1993bytes SHA
2d88050128e908dff438f368d7214815ad870f4c6131bc97407b34eb11022230 matches
the prior qualified report exactly. Runtime qualification222processes/4blobs;
source qualification1518processes/13blobs; each injected one mutation.
The four separate current/stale control cases remain running. This is not a
capture receipt, calibrated score, source optimization or production acceptance.

Existing repair and ordinary delivery regressions passed33tests/2files, zero
skipped,99.08seconds. New passed-seed pure proof plus old proof115tests passed.
Synthetic scored transport with real Git branch/recovery passed9cases on Node22;
parent Node24 rerun and actual worker/evaluator acceptance are now underway.

## Compiler gate continuation

The preceding running-state notes are historical: native87267 finished all five
cases successfully; scoring/delivery source was checkpointed at3ba66add.
The current uncommitted packet adds a fixed compiler child and pinned whole-project
snapshot before scoring. Parent Node24 checker/authoring gate passed49tests/2files,
zero skipped,1.33seconds. Actual read-only authoring passed725roots/1082files,
22,452,362 serialized JSON bytes; no snapshot was installed. Source/web types,
documentation links and lane classification passed (342 real-IO,704 unit files).

Independent review found and addressed compiler deadline renewal across durable
reservation work and UTF-8 BOM stripping at the selected-source boundary. The
owner now retains wall and monotonic compiler deadlines and an outer abort timer,
and preserves exact decoded source bytes. Package review required exact compiler
dependency closure and source/compiler byte continuity during asynchronous builds.
Focused regression and compiled-package verification remain in progress.

One lint run rejected unqualified setTimeout/clearTimeout in the owner; explicit
node:timers imports corrected it and the scoped lint rerun passed. One earlier
atomic patch failed to match a commented lane entry and was reapplied with exact
context; no partial changes or cleanup followed that failed patch.
