# Trusted improvement loop

Base: clean `auto/p00` at `c3b09fece17df1d0b17ccec39ca122eef20dbb8d`.
Previous goal turn was progress: committed runtime repair, real native evidence,
204-test integration gate and a clean build. The broad goal remains active.

- [x] Explore the minimum connected calibration, trusted scoring and candidate-scope contracts with three agents.
- [x] Implement the justified connected slice in an isolated worktree, preserving the diagnostic builtin and existing campaign/delivery paths.
- [x] Independently review and verify exact pins, repeated-evidence attribution, regression refusal, and supported installed execution behavior.
- [x] Integrate tested code and update canonical documentation with actual evidence and remaining commissioning requirements.

The global KILL switch was active at the prior handoff. Do not clear or redirect
it to manufacture a live baseline. No provider/account/activation changes or
GitHub Actions. Local hermetic regression fixtures remain separate from fleet
operation. A new scoring implementation is not commissioned until three actual
repeatable baseline captures and full exact-candidate acceptance exist.

## Status

Integrated retained-capture calibration and candidate comparison at
`67f662fc0ef8e1eee6ffce072b99a217120d7559`, with exact recorded launch semantics
at `713bff6c2f787861975c4e1243beb0275bfa1e4c`. Final rebuilt integration gate
passed 474 tests / 15 files / zero skips in 16.68 seconds. All test handles are
terminal. This bounded evidence increment is complete; the broad autonomy goal
remains active. See delivery.md and the canonical verification artifact.

This is a diagnostic evidence path, not a commissioned score. The current read-only
KILL observation is healthy/active; no switch, provider, account or service changes.

## Errors

- Initial source lookups guessed nonexistent CLI/index locations; corrected through
  repository search. A later zsh glob for a kill-switch filename did not match;
  the driver pointed to `src/core/sandbox/policy.ts`.
- Initial real-filesystem comparison fixtures omitted required staging directories
  and canonical newlines. The reader correctly refused them. Fixtures were fixed,
  not the reader; the 30-case filesystem suite then passed.
- Entire resume found no checkpoint for the isolated branch; Entire is enabled in
  manual-commit mode. No checkpoint recovery claimed.
