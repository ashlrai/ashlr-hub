# Enrollment findings

The selected local account configuration validates against current schemas.
Personal and CMP use a supported General model, Claude uses Opus. Personal is
paused, allocation is 75%; saved evidence admits no workers. No Spark alias exists.
Existing pool evolution preserves receipts, revisions, shared limits and pauses.
Existing quota-scope access can reserve General separately after migration.

The current quota collector witness is expired and a legacy v1 pending record
cannot establish native-process custody. Starting collection is not a safe repair.
Claude's zero-inference /usage display can originate in cached headers and is
intentionally excluded from admission. Genuine rate-limit event evidence remains
the existing supported import route. No native clients or providers were invoked.

## Verified implementation

Three agents handled pure implementation/exploration, independent tests, and cold
review. Parent implemented the private publication backend and CLI. New CLI:
`resources pool spark prepare`; it never invokes evolution apply. Existing
quota-scope access, additive evolution and quota validators remain authoritative.
Unknown fields/getters, unsafe input paths, preexisting output, directory
replacement, source drift and equivalent-JSON raw-byte changes are refused.

Real proposal directory: local private artifact
`/Users/masonwyatt/.codex/artifacts/ashlr-spark-enrollment.ndJdDt/proposal`.
No bindings, account hints or commands copied into tracked evidence. Metadata is
retained in readiness.json. Source execution through tsx returned prepared, exit0.
The actual ledger check returned uncertain-work, exit1; its legacy collector
pending marker exists and is independently sufficient for refusal. No apply ran.

## Validation

- Pure planner:31/31 tests, no skips.
- Pure planner + CLI + existing evolution CLI:87/87,3 files,1.45s, no skips.
- Real private publication:16/16,1 file,1.40s, no skips.
- Existing evolution/scope boundaries:48/48,2 files,19.13s, no skips.
- The selections overlap the standalone planner suite:151 distinct tests total,
  not182. These are not provider acceptance or a benchmark-quality claim.
- Final combined gate87908:151/151 passed across all six suites, zero skips,
  exit0,20.97s. Strict direct CLI-test import and final documentation checks also
  passed on handle30336. Both handles are terminal and must not be polled again.
- Full source typecheck, scoped source/test ESLint, strict independent test-import
  checks, lane classifier and documentation link checker passed.
- TypeScript emission in this isolated worktree passed; compiled CLI help passed.
  No full web/native-bundle build was performed here; primary dist stayed fixed.

## Passive collector inspection follow-through

Existing acquisition fallback was not an inspection-only operation: when valid
it can start native clients. The new collectorInspection projection is separate
from metadataCollector and exists only in no-execution, no-native-config snapshots.
It reuses the private marker codec without lease acquisition or recovery. Valid
v2+ records say recovery-not-evaluated; v1 explains missing original custody;
absence never establishes readiness. Strict web decoding rejects contradictory
states/versions and private-valued or extra fields. Historical UI uses existing
polling rather than another timer or request.

Existing recovery cannot reconstruct v1 original host/process identity from a
timestamp or missing lock. Cold exploration proposed a possible pre-reboot
custody witness plus later verified same-host different-boot reconciliation;
that is not implemented, not authorized as a machine reboot, and not permission
to clear current records. Actual global/account controls remain independent.

Visual acceptance used a temporary loopback, read-only server with actual saved
account configuration and no native metadata flags/control token. The real legacy
diagnosis, saved75%ceiling and whole-personal pause rendered correctly. Screenshot
artifacts are private and not committed. The server and temporary tab were closed
after testing stale-state behavior. This was a preview, not a fleet activation.

The frontend-design skill kept the addition in the existing theme and typography;
the React review kept existing polling, direct imports, semantic headings and
no new effects/controls. Engineering-documentation guidance separates passive
inspection from collector startup and configuration from provider readiness.
