# Source-backed settlement findings

The previous goal turn was progress: scoped histories now retain one shared
ledger, source694b568e built cleanly and186tests passed. All previous handles are
terminal. A standing mission still lacks a proven predecessor check and caller.

The persisted supervisor records originaldeadline/entries/attemptoutcomes but its
public snapshot projects cached state. Extract existing validator into a read-only
reader, preserving owner restart semantics. Delivery proof similarly needs the
durable part of successorSource without constructing an execution owner.

Configured successor journal requires independently derived pins, not pins read
back from itself. Every intent needs completed accounted output, then either
explicit stop or prepared/admitted/completed successor. Full registry membership
must match the queue; closing a process does not resolve unadmitted obligations.

Implemented composition uses the project's pinned workspace for successor
proposal custody, not the runtime's generation transport directory. It compares
the exact canonical acceptance/source context used by the console wrapper.
The outcomes reader supplies existing generation-receipt joins; missing or
changed receipts hold completion while unknown token/timing measurements remain
unknown. Current queue entries must be settled and every graph must independently
be complete. Every configured proposal intent must end in an accounted stop or
an exactly matching prepared, admitted and completed child.

The first checker requires one connected successor chain rooted at the original
setup. Extra ordinary objectives, ambiguous tips and unlinked registrations are
held, not guessed into a new seed. It is read-only historical evidence sampled
twice; future mission publication must own its scope and revalidate. It does not
claim atomic exclusion, rollback resistance, fresh quota or dispatch authority.

Independent review found and closed three integration gaps: generation receipts
were initially not joined, controller execution/Universe run locks were missing,
and proposed child name/objective needed exact registration equality. Campaign-
attributed runs now also require a matching durable campaign step. The accounting
fingerprint excludes time-dependent scheduling previews and sample timestamps.

Parent regression gates passed:19696=63tests/3files,56680=248tests/7files.
Static39372 full source types, changed source/test ESLint and diff checks passed.
The first actual scoped diagnostic59071 delivered both campaigns at215.21s,
passed the original new checker assertions, but synchronous offline inspection
delayed restart until376.41s and exceeded the encompassing360s test timeout.
Final tests move offline inspection after original restart verification and
expand only the overall test budget; product runtime/deadline policy is unchanged.
Per-check timing is now required. Inspection responsiveness is a known limitation.

Final actual source-CLI setup acceptance is still in progress. No provider, service, account, global
stop, collector recovery, public site, registry or remote Git mutation occurred.

## Next executable increment (proposal, not implemented)

A foreground standing-mission owner should persist one original mission deadline
and allowance, run an existing finite console scope, drain/close it, inspect this
predecessor proof, and create a deterministic new scoped setup from its exact
delivered tip. Between-scope proposals must use accounted existing worker tasks,
retained task/receipt/output identities, and the fixed proposal schema; uncertainty
must not trigger blind retry. An explicit stop result ends the mission.

Reuse the existing console execution/collector lifecycle rather than implement a
second scheduler. Decide whether to use its existing CLI/loopback surface or
extract a shared host session after inspecting close/custody behavior. Quota
collector ownership must be absent during setup publication and reacquired by
the next session without weakening freshness vetoes. Setup publication needs an
action-time mission lease/stop/deadline/predecessor guard. A green historical
check alone must not be the publication authority. Initial real acceptance should
cross two scopes on one ledger, preserve evaluator/account policies and deadline,
deliver from the exact previous commit, then stop and restart without replay.
