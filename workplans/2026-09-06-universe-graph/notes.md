# Evidence and decisions

## Current source

- Latest upstream master before work: 77741f9518cd3ea6a16057ce9692e898126b5ed2 (Universe delivery PR #356).
- Prior delivery feature head: f835c269fb3aab759d0d9fce45ccaf0cdbb45aea; its tree matches that merge.
- Public distribution remains separate from merged source. Prior npm authentication was E401; this turn has not attempted publication.
- Entire enabled in manual-commit mode; resuming prior Universe branch found no checkpoint.

## Research underway

- Explore: existing knowledge/mission graph primitives and Universe integration shape.
- Evidence: lineage identities, degradation, boundedness, and agent-useful queries.
- Acceptance: API/CLI/console and packaged verification patterns.
- Root: source recovery, architecture coordination, integration and final acceptance.

## Settled graph contract

- Parent code ancestry and feedback ancestry are different edges. A failed preceding attempt can provide feedback while the parent remains an older successful elite.
- Trial and artifact nodes represent occurrences scoped to Universe/run/trial; equal content digests do not merge occurrences or fabricate cycles.
- Opaque deterministic node identities survive public metadata redaction without exposing reversible secret-shaped source IDs. Exact provenance fields remain subject to normal API privacy filtering; SDK/CLI retain local evidence.
- Existing knowledge graph silently degrades to partial/empty data and uses basename IDs; it is not an evidence-authority primitive. Existing mission graph is a useful boundedness pattern but represents different semantics.
- The targeted reader reuses a single validated Universe summary for matching campaign projections, scans only bounded campaign creation points for membership, and verifies delivery repositories for the selected Universe only.
- Historical artifact digests are recorded evidence, not freshly rehashed bytes. Current delivery verification has its existing stronger semantics; neither is a production acceptance claim.
- A question offered evidence lineage first versus cross-project scheduling. Evidence lineage is the stated default and remains within the requested graph-engineering scope.

## Discovery command corrections

Some initial searches named nonexistent web/data helper paths and one unmatched shell glob. Corrected by locating the existing API, data queries, public-json/scrub utilities and test membership file. No product failures or state changes resulted.
