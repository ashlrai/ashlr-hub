# Universe yield-comparison continuation

## Goal

Make candidate-generation changes measurable against a matched control, preserving verified evidence and distinguishing local evaluator progress from accepted engineering delivery.

## Phases

- [x] Establish current source, worktree ownership, memory and Entire context.
- [x] Parallel exploration of existing comparison, accounting and native acceptance patterns.
- [x] Confirm a bounded implementation contract and strategic direction.
- [x] Implement independent core, interface and acceptance lanes.
- [ ] Verify locally, inspect the exact package, publish source and hand off.

## Boundaries

- Start from merged PR359 master 245747e0239dbfd90e01d0d1824aa0c0e14a0eae.
- No GitHub Actions, npm publication, account changes, resident activation, or automatic scheduling changes.
- Preserve the primary Desktop checkout and all legacy receipt/version behavior.
- Comparison must not infer customer acceptance, useful business value, token savings or model identity from incomplete measurements.
- No model requests during exploration. Root may later evaluate a bounded, explicit local-only fixture if justified; no paid provider fallback.

## Status

Implementation and integrated local verification complete; preparing clean-source
package acceptance and source publication. Post-commit identities and final
publication status will be retained in the external release handoff.
No new executor, scheduler, feedback protocol or accounting authority.

## Implementation contract

1. Expose readUniverseCampaignComparison(baselineId, challengerId, options) and a pure buildUniverseCampaignComparison over detached, validated source snapshots. Add strict `ashlr universe compare <baseline> <challenger> --root <path> --json`.
2. Preserve exact comparator digests; do not normalize expanded evaluator paths. Report mismatch conservatively. Compare ordered variants, per-run budgets and campaign budgets separately from observed feedback conditions and executed workload.
3. A feedback-disabled versus v2 pair is a bundled feedback contrast, not isolated search-context uplift. Keep unmatched descriptive results visible, but suppress comparative advantage when controls or workload do not match.
4. Scope all counts and final per-niche retained scores to durable campaign step/run occurrences, not global current elites. Require fresh generation-1, fully attributed, completed nonempty work for normalized rates. Flag prior/interleaved/later unrelated history; never credit it to the campaign.
5. Separate first admissions, strict improvements and distinct selected recorded artifact digests excluding the seed. Include all attempted work in resource denominators. Missing usage, zero denominators and incomplete work yield null ratios, never inferred savings or accepted changes.
6. Expose summed recorded run duration separately from wall-clock span. Delivery counts require independent existing delivery verification and exact occurrence attribution; deduplicate artifacts across branch receipts. Accepted production changes remain unavailable.
7. Target only requested campaigns and their Universes. Use bounded read-only snapshot checks, no directory creation or execution locks. Repeated CLI/SDK reads must preserve ledger bytes, Git state and request count.
8. Reuse existing native fixture, replay and installed-package patterns. Update the canonical guide and local package smoke; no new dashboard, database, dependencies or release-policy changes.

## Errors and observations

- Entire resume found no checkpoint on either old or new branch.
- Port 56322 has no listener. Ollama metadata endpoint is reachable; listing models did not start inference.
- An exploratory campaign-types.ts lookup had no file; campaign types are in types.ts.
- A guessed read-model.ts lookup had no file; the existing public overview is in overview.ts.
- The first package smoke incorrectly included overview sampledAt in its no-mutation
  assertion. Compare stable evidence while retaining Git-ref and request checks.
