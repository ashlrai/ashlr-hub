# Findings

The capture increment retained a real failed-check report and verified replay,
but its full-success path has only mocked producer coverage. The installed
workload separately has full native correctness/repeatability evidence. This
increment must join those two layers with retained native success evidence.

Source authority: the current runtime, `artifacts/hub-verification-benchmark-plan.md`,
`src/core/universe/preparation-measurement-capture.ts`, and existing campaign /
delivery code. Prior report counts are not a newly frozen baseline.
# Current findings

- The existing campaign/archive/delivery supports genuine baseline then changed
  generation with real parent linkage; no replacement scheduler is needed.
- A future trusted score must require all 19 correctness checks, exact four-leaf
  and eleven-request topology, no per-scenario regression, and sum only leaf plus
  workflow process counts (blob counts are subtotals).
- The existing trial finally block deleted evaluator scratch even if builtin
  settlement was uncertain. A durable per-trial intent/settlement guard is needed
  before scoring can safely use that path. A final failed run alone cannot prove
  its evaluator's independently owned process groups stopped.
- Full capture attempt was withheld by the global KILL switch. Keeping it intact
  is distinct from running isolated automated regression fixtures.
- Actual custody acceptance exposed a preexisting storage lifecycle gap:
  frozen nested fixture seeds can prevent best-effort scratch deletion. The
  runtime may record confirmed process settlement and continue correctly while
  retaining protected files. No unbounded recursive permission change was added.
  Future storage reclamation must preserve the distinction between settled
  scratch and unresolved process evidence.
